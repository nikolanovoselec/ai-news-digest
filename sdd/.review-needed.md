# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-25 — HIGH: Admin force-refresh endpoint has no REQ

**Trigger:** commit `b65339e` changed the GET response shape of `/api/admin/force-refresh` (browsers now get a 303 to `/settings`, scripts opt into JSON via `Accept: application/json`).

**Finding:** No REQ in `sdd/` documents the operator force-refresh contract. The source file `src/pages/api/admin/force-refresh.ts` now carries three annotations — `Implements REQ-PIPE-001`, `Implements REQ-OPS-004`, and `Implements REQ-AUTH-001` (the REQ-AUTH-001 annotation was added in the rate-limit hardening branch and is correct because the file enforces the three-layer admin gate). REQ-PIPE-001 and REQ-AUTH-001 are accurate cross-cuts, but the file's primary subject — the operator-triggered out-of-band scrape tick with REUSE_WINDOW_SECONDS guard and content-negotiated 303-vs-JSON transport — is not described by any REQ. The lingering `REQ-OPS-004` annotation is also still incorrect (REQ-OPS-004 is about crawler policy, not operator tooling). The Admin actor is named in `sdd/README.md` as having "force a fresh scrape tick" capability, but no REQ defines the endpoint's behaviour, transports, or auth gate.

**Latest state (2026-04-27):** `head -3 src/pages/api/admin/force-refresh.ts` shows the three current annotations. No code change to the endpoint's contract this push — the gap is structural and unchanged.

**Why escalated (not auto-fixed):** Creating a new REQ across domains is a JUDGMENT call. Conservative unleashed rule: never invent a new REQ when domain placement is ambiguous (observability vs generation).

**Proposed resolution (please pick one):**
1. Add `REQ-OPS-005: Admin force-refresh endpoint` in `sdd/observability.md` covering both transports (303 default, JSON on Accept), Cloudflare Access gating, and the REUSE_WINDOW_SECONDS guard. Update annotation in `force-refresh.ts` from `REQ-OPS-004` to `REQ-OPS-005`; keep `REQ-PIPE-001` and `REQ-AUTH-001`.
2. Fold the contract into a widened `REQ-GEN-002: Manual refresh` and split user-facing vs operator-triggered paths.
3. Treat as out-of-scope operator tooling and remove the misleading `REQ-OPS-004` annotation from the source file (keep `REQ-PIPE-001` and `REQ-AUTH-001`, drop `REQ-OPS-004`).

## 2026-04-27 — HIGH: Stale "7 days" prose left behind by PR6 retention extension (code-reviewer / doc-updater lane)

**Trigger:** PR6 (commit `117a527`) extended REQ-PIPE-005 retention and REQ-HIST-001 history window 7 → 14 days. The spec, the runtime constants (`RETENTION_SECONDS = 14 * 86400` in `src/queue/cleanup.ts`, `WINDOW_SECONDS` in `src/pages/api/history.ts`), and the user-visible empty-state copy in `src/pages/history.astro` (`No articles in the last 14 days.`) are all updated. But PR6 left several stale "7 days" / "7-day" references in source comments, test descriptions, test assertions, and `documentation/`.

**Spec-reviewer cannot fix these** — source code, tests, and `documentation/` are outside the `sdd/` lane. Spec-reviewer fixed the one stale reference inside `sdd/` (`sdd/generation.md` domain header line 3, "7-day retention cutoff" → "14-day retention cutoff") and added the required changelog entry.

**For code-reviewer / developer (source code + tests):**
- `src/queue/cleanup.ts:8` — comment block introducing pass 1 says "articles older than 7 days are deleted" — should be 14.
- `src/worker.ts:17` — cron-table comment names REQ-PIPE-005 by REQ ID (no day count) — verify nothing leaks 7 there.
- `src/pages/history.astro:93, 150, 852` — three comment blocks reference "the 7-day window" / "the full 7-day list" / "Count tag occurrences across every article in the 7-day window".
- `tests/pipeline/cleanup.test.ts:111` — test name: `it('REQ-PIPE-005: deletes articles older than 7 days when no user has starred them', ...)`. The test still exercises the right behaviour (uses `daysAgo(8)` as the stale article and `daysAgo(1)` as the fresh one — both still on the correct side of the 14-day cutoff) but the title is misleading. Stale window-edge coverage: there is no test at the 14-day boundary specifically (e.g. `daysAgo(13)` should survive, `daysAgo(15)` should be deleted). Consider widening the test to lock the new boundary or rename the existing one.
- `tests/history/api.test.ts:3, 184` — module-level docstring and inline comment both say "last 7 days" / "7-day window".
- `tests/history/page.test.ts:133-134` — **test will fail in CI**: `expect(historyPageSource).toContain('No articles in the last 7 days.')` — the shipped page now renders `No articles in the last 14 days.` so this assertion is broken. Update the literal to `'No articles in the last 14 days.'`

**For doc-updater (`documentation/`):**
- `documentation/configuration.md:75` — cron table row says "removes articles older than 7 days" with the link target already updated to the new `#req-pipe-005-fourteen-day-retention-...` anchor. Prose needs to follow.
- `documentation/architecture.md:170` — `src/worker.ts` row says "daily 03:00 UTC tick fires the 7-day retention cleanup".
- `documentation/architecture.md:174` — `src/queue/cleanup.ts` row says "deletes unstarred articles older than 7 days" in the (1) pass description. The (2) stuck-tag prune correctly retains its 7-day TTL — that is a separate window (`STUCK_TAG_TTL_SECONDS = 7 * 86400`), not retention; do not edit it.
- `documentation/architecture.md:265` and following — flow diagram describing pass 1 — verify no 7-day numeric leak.

**Why escalated:** Each stale reference is a one-line edit, but the rule that lets spec-reviewer make them — that the file is in `sdd/` — does not apply. The right hand-off is doc-updater (which runs after spec-reviewer in the same push) for the `documentation/` lines, and the next code-reviewer pass for the source/test lines.

**Coverage gap (also for code-reviewer / tdd-guide):** The new REQ-HIST-001 AC 8 ("history window matches retention; both are 14 days") is testable in principle — a regression where one window drifts from the other would silently re-introduce the empty-rows-or-hidden-data hazard the AC names. No test references AC 8 specifically. Consider either (a) a unit test that asserts the two constants are derived from the same source, or (b) an integration test that inserts an article at `daysAgo(13.5)` and confirms it surfaces on `/history` and is not retention-deleted on the 03:00 cron tick.
