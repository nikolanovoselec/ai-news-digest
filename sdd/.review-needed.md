# Review Needed

Findings escalated by spec-reviewer or doc-updater that require human attention. Resolve and clear entries as they are addressed.

## Round limit reached — 2026-04-29 (re-confirmed)

Spec-reviewer self-limited per the 2-round rule on a second consecutive invocation. The last 3 commits are:

1. `f30d276 fix(review): code-reviewer v5 highs` (authored by `doc-updater`, no `[autonomous]`/`[unleashed]`/`[spec-reviewer]` tag in the subject — does not count toward the round counter)
2. `f2cb7a1 [unleashed] [spec-reviewer] chore(spec): refresh last-clean-run audit log`
3. `b74cb1c [unleashed] [spec-reviewer] sync(spec): json_ld_defence`

Two of the last three subjects carry the `[spec-reviewer]` tag and target the same `sdd/` json-ld category, so this run halts before any further edit. Note: `f30d276` is an agent commit by author but the round-counter rule keys on the subject tag, not the author — by the letter of the rule the counter is tripped. The counter resets when a non-agent commit lands on `review-2026-04-29/security-and-critical` whose subject also lacks the agent tag, OR when the user pushes a manual edit.

### What this run would have done if not round-limited

A full re-review of `git diff origin/main...HEAD` confirms the spec is **almost entirely in sync** with the branch — the prior `b74cb1c` run completed Phase 1 + Phase 2 cleanly. One LOW-severity drift was introduced after that run by `f30d276 fix(review): code-reviewer v5 highs`:

- **REQ-OPS-004 AC 6 wording narrower than shipped defence.** AC 6 currently reads "rewrites any `</script>` sequence to its escaped form (case-insensitive)". The shipped helper `src/lib/json-ld.ts` now performs a per-character `<>&` → `\uNNNN` escape that defeats not just `</script>` but also `<!--` (script-data escaped state), `<![CDATA[` / `]]>` (XML-mode CDATA), and `<script` re-entry. The contract still holds — every value in those scenarios is correctly escaped — but the AC understates the breadth of the defence. **Proposed minimal edit (deferred to next round)**: replace "rewrites any `</script>` sequence to its escaped form (case-insensitive)" with "rewrites every `<`, `>`, and `&` byte to its `\uNNNN` JSON form, defeating every HTML state-transition vector that could escape the script block (`</script>`, `<!--`, `]]>`, `<script` re-entry)". Tests already pin the broader behaviour (`tests/lib/json-ld.test.ts` has dedicated cases for `</script>`, `<!--`, and `]]>`), so coverage is not the gap — only the prose AC.

The source-vs-test coverage check still passes for every REQ touched by the security/critical PR:

- **REQ-OPS-003 AC 1** — CSP `style-src` tightened to `'self'` (no `'unsafe-inline'`). Source: `src/middleware/security-headers.ts`. Tests: `tests/observability/security-headers.test.ts` (byte-for-byte CSP) and `tests/e2e/view-transition.spec.ts` (live CSP enforcement during article-detail navigation).
- **REQ-OPS-004 AC 6** — JSON-LD `</script>` defensive escape via `safeJsonLd`. Source: `src/lib/json-ld.ts` + `src/layouts/Base.astro`. Tests: `tests/lib/json-ld.test.ts` (3 tests covering simple values, lowercase `</script>`, and case-insensitive `</SCRIPT>`).
- **REQ-AUTH-001 AC 8a–c** — three-layer admin gate with structured AUD-unset warning. Source: `src/middleware/admin-auth.ts` (added `lastAudWarnAt` hourly throttle and `admin.auth.aud_unset_warning` log event). Tests: existing `tests/middleware/admin-auth.test.ts` plus the `LogEvent` enum guard in `tests/observability/log-events.test.ts`.
- **REQ-AUTH-001 AC 9a–f** — rate-limit coverage broadened beyond auth+mutation to authenticated polling. Source: `src/lib/rate-limit.ts` (`SET_TZ`, `DISCOVERY_STATUS` rules) wired into `src/pages/api/auth/set-tz.ts` and `src/pages/api/discovery/status.ts`. Tests: `tests/auth/set-tz.test.ts` and `tests/settings/discovery-status.test.ts` carry the in-memory KV stub so the limiter's fail-open path no longer silently swallows a `TypeError`.
- **REQ-SET-006 AC 5** — discovery-progress polling rate-limit. Source: `src/pages/api/discovery/status.ts`. Test: `tests/settings/discovery-status.test.ts`.
- **REQ-SET-007 AC 7** — timezone-update rate-limit. Source: `src/pages/api/auth/set-tz.ts`. Test: `tests/auth/set-tz.test.ts`.

Behavioural deltas this branch ships that are **not** REQ-bearing (correctly, per spec discipline):

- `src/lib/email-html.ts` — pure refactor extracting `escapeHtml` + `headlineRow` from `src/lib/email.ts`. No behaviour change; the email render path's HTML-escape contract is already in REQ-MAIL-001.
- `src/pages/api/digest/[id].ts` collapsed to `410 Gone` — already covered by the 2026-04-29 changes.md entry "Per-digest retrieval endpoint retired alongside REQ-READ-004's earlier deprecation". The endpoint stopped being the source of truth on 2026-04-23 with the global-feed rework; today's commit just retires the dead route. REQ-READ-004 is already `Deprecated`.
- `src/pages/history.astro` — `innerHTML = ''` swapped for `replaceChildren()`. Identical user-visible behaviour, safer DOM API. No spec impact.
- `src/scripts/theme-toggle.ts` — `theme` cookie now carries `Secure`. The `Secure` attribute is implementation-level cookie hardening; REQ-DES-002 AC 2 already requires "persists the choice for the current browser" without specifying flags. Correct to keep the flag out of the REQ.
- `src/styles/global.css` — pinned `html[data-theme]` background to literal hex (replaces the inline `style=` attribute that CSP `style-src 'self'` now rejects). Same first-paint invariant as REQ-OPS-004 AC 5; `tests/observability/crawler-policy.test.ts` was rewritten to assert on `GLOBAL_CSS` instead of the inline attribute. No spec impact.
- `src/components/DigestCard.astro` — removed the inline `style="--stagger-delay: …"` attribute. The stagger animation lived in the now-Deprecated REQ-READ-004; the component still emits `data-stagger-index` for downstream consumers. `tests/reading/digest-page.test.ts` was rewritten to assert on the `Math.min(index, 9)` clamp without the inline-style attribute. No spec impact.

### Optional follow-ups (LOW, not blocking)

- ~~`src/lib/json-ld.ts` and `src/lib/email-html.ts` lack `// Implements REQ-X-NNN` annotations~~ — **resolved** in `f30d276`: both files now carry the top-of-file `// Implements REQ-OPS-004` and `// Implements REQ-MAIL-001` annotations.
- REQ-SET-006 has 5 ACs but only 4 dedicated tests in `tests/settings/discovery-status.test.ts`; the new AC 5 (rate limit) is exercised structurally by the in-memory KV stub but has no dedicated 429-on-exhaustion test. Consider adding one when the round-limit clears. Same call for REQ-SET-007 — many tests exist, but the rate-limit branch is not pinned by an explicit assertion.

### Resume

Push any commit on this branch whose subject does NOT contain `[autonomous]`, `[unleashed]`, or `[spec-reviewer]` — a real user edit, a manual REQ-OPS-004 AC 6 reword per the proposed minimal edit above, or any other unrelated change — to reset the round counter, and the next push triggers a fresh spec-reviewer run.
