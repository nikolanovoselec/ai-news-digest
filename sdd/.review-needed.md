# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-23 — Residual TDD gap after global-feed rework

spec-reviewer post-rework sweep promoted REQ-PIPE-001..006, REQ-STAR-001..003, REQ-READ-001/002/005, REQ-HIST-001/002, REQ-MAIL-001, and REQ-AUTH-001 to Implemented — each has both source and test references. REQ-READ-004 is Deprecated (replaced by REQ-PIPE-001) because the shared article pool removes the per-user in-progress state it described.

One Partial residue survives the rework:

### REQ-SET-002 (settings — hashtag curation)
- **AC 2** — selecting a chip inverts its colour scheme, reveals the red × affordance, and `aria-pressed` toggles. Needed: a reading-surface test that asserts the selection-toggle handler and the inverted-chip styling are present.
- **AC 4** — toggling selection never POSTs to the tags endpoint; only delete does. Needed: a unit test that imports the tag-strip script and confirms the "body click vs × click" handler split.
- **AC 7** — tag-strip selection filters the grid; empty selection shows every card; no match shows a "no stories match" region. Needed: a test asserting the filter walk over digest cards and the empty-state region markup.
- **AC 8** — new accounts are seeded with the default hashtag list; settings exposes a "Restore initial tags" action. New-user seeding is now covered by `tests/auth/callback.test.ts` and `tests/lib/default-hashtags.test.ts` — only the settings-page "Restore initial tags" handler remains without a test.

Invoke `tdd-guide` to close the remaining AC 2/4/7 gaps, then promote REQ-SET-002 to Implemented.

### REQ-SET-004 (settings — model selection)
Stays Partial for product reasons (Notes in the REQ): the global pipeline uses a single default model, so ModelSelect is hidden in the UI while the underlying code remains. No test action required — this is a behaviour decision, not a coverage gap.

## Round counter

Round 1 after the 16-commit global-feed rework push. A non-agent commit (`chore: retarget orphan REQ-GEN-* annotations`) is the most recent, so the round counter is fresh — agents may auto-fix on the next round once the REQ-SET-002 gaps close.
