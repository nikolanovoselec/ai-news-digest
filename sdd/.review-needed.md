# Review Needed

Items requiring human attention. Clear entries as they are resolved.

## 2026-04-24 — REQ-PIPE-001 AC 4 (test gap)

The extended AC 4 now describes a new observable behaviour — re-seen canonical URLs get their last-feed-sighting re-stamped on every scrape tick — but no test yet asserts that the coordinator performs this UPDATE when a feed re-emits an existing article. The REQ remains `Implemented` because its other AC bullets are covered and the REQ ID is referenced in coordinator tests, but a dedicated regression test is warranted so the 4-hour-stale-dashboard bug cannot silently reappear.

Suggested next step: invoke `tdd-guide` against REQ-PIPE-001 AC 4 to add a coordinator test that seeds an existing article with a stale `ingested_at`, re-emits its canonical URL through the feed fetch, runs the coordinator, and asserts the `ingested_at` on the existing row is bumped to ~now.
