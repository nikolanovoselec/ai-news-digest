# Review Needed

Findings escalated by spec-reviewer that require human attention. Resolve and clear entries as they are addressed.

## skipped-test:REQ-READ-002,REQ-HIST-001 — perf-comparability test (MEDIUM)

- File: `tests/e2e/view-transition.spec.ts:230`
- Test name: "history back-nav is comparable to digest back-nav — accepted as structurally slower"
- Status: permanently skipped via `test.skip(...)`
- Context: this push deliberately skipped the median-of-3 perf-comparability test because the user accepts that `/history` is structurally slower than `/digest` (more cards in opened day-groups) and the structural morph-pair contract tests above it are the regression we actually care about. The skipped test is one of many tests covering both REQs — both have plenty of non-skipped coverage in this file and elsewhere.
- Suggested resolution: if the perf gap is genuinely accepted as out of scope, add a `skipped-test:REQ-READ-002,REQ-HIST-001` entry to `sdd/.user-overrides.md` so future spec-reviewer runs stop flagging it. If the gap is something to revisit, leave this entry in place as a reminder.
