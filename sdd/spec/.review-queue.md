# Review Queue

## 2026-06-15 — Source-anchor truth gaps from PR-boundary review 094cdbfe8c29

Open findings:

- `REQ-READ-006 AC 1` has no verified source anchor after removing the false `src/pages/offline.astro::offline-page` anchor. The source tree does not currently contain the specified `No stories today — try broader hashtags` + `/settings` failure page behavior, so this must be resolved by implementing the behavior or revising the requirement against source truth.
- `REQ-DES-001 AC 4` and `REQ-DES-001 AC 5` had false `src/styles/global.css::--color-bg` anchors removed. If the framework requires source anchors for CSS selector behavior, add selector-aware source-anchor support before re-anchoring them.
