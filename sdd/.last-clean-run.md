# Last `/sdd clean` Run

**Date:** 2026-05-13
**Mode:** unleashed
**Scope:** all (full `sdd/` + `documentation/` corpus)
**Branch:** develop
**Triggered by:** user (`/sdd clean --all`)

## Pre-run state

Working tree clean. Last commit `ee14dfd refactor(spec): split authentication by actor coherence, add rate-limits domain` landed the actor-coherence + Rules-A-B-C + flat-Pass-1-12 rule refactor and produced a tuned corpus dry-run of 0 HIGH / 2 MEDIUM / 7 LOW.

## Findings

| Severity | Count | Auto-fixed | Escalated | Informational |
|---|---|---|---|---|
| CRITICAL | 0 | - | - | - |
| HIGH     | 0 | - | - | - |
| MEDIUM   | 2 | 1 | 2 | - |
| LOW      | 7 | 1 | - | 6 |

### Auto-fixed (SAFE)

1. **`sdd/generation.md` - REQ-PIPE-001 AC 8 sub-bullet promotion** (MEDIUM, `sub-bullets-banned`). The five sub-bullets `a./b./c./d./e.` describing body-fetch behavior were promoted to top-level ACs 8-13. Subsequent ACs (Google News fan-out, scrape-wait timeout, blocklist) shifted from positions 9/10/11 to 14/15/16. No clause dropped; the parent AC 8 retained as "the pipeline fetches the article body directly," with sub-clauses now siblings.
2. **`documentation/architecture.md` - file-level `doc-allow-large` marker** (LOW, Pass 2 file-budget). File at 442 lines exceeded the 350-line Pass 2 budget. Added `<!-- doc-allow-large: AD46 architecture-doc diagram-section exemption -->` after the audience line. AD46b is the existing carveout that justifies the size (load-bearing ASCII diagrams require contiguous prose context). The three element-level markers at the diagrams remain.

### Escalated to `sdd/.review-needed.md` (JUDGMENT)

1. **REQ-PIPE-001 now has 16 ACs** after the sub-bullet promotion above. Single Admin actor; mixed concerns (coordinator-level orchestration vs body-fetch sub-feature). Two split candidates documented in `.review-needed.md`; conservative recommendation is to extract body-fetch ACs to a new REQ-PIPE-009. Defer to owner.
2. **REQ-PIPE-003 has 17 ACs.** Single actor (Admin); single concern (same-story dedup). No clean actor or cross-cutting-concern axis. A semantic split would require Intent re-interpretation, which is the JUDGMENT case the rule reserves for owner decision.

### Informational (no auto-fix per rule)

- `sdd/email.md` - REQ-MAIL-001: 10 ACs, 27 lines.
- `sdd/generation.md` - REQ-PIPE-001: was 11 ACs / 32 lines, now 16 ACs after promotion; REQ-PIPE-003: 17 ACs / 34 lines.
- `sdd/reading.md` - REQ-READ-007: 8 ACs.

Length and AC-count smells on single-actor, single-concern REQs are reported but not auto-fixed (rule: "no auto-fix without a split axis").

## Rules applied this run

- `spec-discipline.md` - Sub-bullets in ACs are banned (mechanical promotion).
- `spec-discipline.md` - AC count guidance with co-occurring-violation severity (>10 ACs without clean axis triggers JUDGMENT escalation, no auto-fix).
- `documentation-discipline.md` - Pass 2 file-budget enforcement (file-level `doc-allow-large` marker added per AD46b).
- `spec-discipline.md` - Deprecated-REQ carveout held (no structural findings emitted on Deprecated REQs).
- Rule-A (stub-after-extraction), Rule-B (Notes two-shape), Rule-C (preamble edit-history): zero new findings across the corpus.

## Working tree changes

- `documentation/architecture.md` - 2-line insertion (file-level marker plus blank).
- `sdd/generation.md` - REQ-PIPE-001 AC 8 expanded; subsequent AC renumbering.
- `sdd/.review-needed.md` - 2026-05-13 section appended with two JUDGMENT escalations and one informational summary.
- `sdd/.last-clean-run.md` - this file overwritten.

No changes to `documentation/decisions/README.md`, `sdd/README.md`, `sdd/changes.md`, or any test file. Round counter ends at 1; the `[sdd-clean]` commit prefix is excluded from spec-reviewer's 2-round spiral guard.

## Next steps

- Owner reviews the two MEDIUM escalations in `sdd/.review-needed.md` (REQ-PIPE-001 split, REQ-PIPE-003 split) and either approves option (1) per item or chooses an alternative.
- No `sdd/changes.md` entry was added: per the changelog-discipline rule, the sub-bullet promotion did not change a user-observable AC (each sub-bullet was already an independent observable contract, just nested) and the doc-marker insertion is operator metadata, not product behavior.
