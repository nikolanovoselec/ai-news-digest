# Last spec-reviewer run

**Date:** 2026-05-11
**Mode:** unleashed
**Branch:** develop
**Trigger:** PR #248 sync (HEAD 51edf5e, base main)
**Round counter:** 0 (no agent-authored commits in last 3)

## Phase 1 — Sync findings

### HIGH 1: REQ-DISC-001 AC 2 drift (spec vs code)

**File:** sdd/discovery.md
**Drift:** AC 2 said tags are picked "ordered by the earliest `added_at` for each tag." The new migration (`0021_pending_discoveries_priority.sql`) adds a `priority` column and the drain query in `src/lib/discovery.ts` now uses `ORDER BY MAX(priority) DESC, MIN(added_at) ASC`. Brand-new accounts enqueue their first tag with `priority=10` so it jumps the steady-state queue.
**User-observable consequence:** a new user's first hashtag now resolves on the next 5-minute cron tick instead of waiting up to 10 minutes behind the steady-state backlog.
**Resolution:** rewrote AC 2 to describe the new ordering in user-observable terms (priority for brand-new-user enqueues, earliest `added_at` within the same priority level). Added changelog entry under 2026-05-11.

## Phase 2 — Validate findings

### MEDIUM 2: Missing REQ annotations on 5 new schema files

The 5 new Zod schema files in `src/lib/schemas/` carried only `// Implements CF-013`. Each one validates input for a route that owns one or more REQs and is therefore co-implementing observable error-code behavior. Per spec-discipline.md, every source file implementing observable behavior must carry the matching REQ annotations.

Annotations added:

- `src/lib/schemas/account.ts` → REQ-AUTH-005
- `src/lib/schemas/discovery-retry.ts` → REQ-DISC-004
- `src/lib/schemas/set-tz.ts` → REQ-SET-007, REQ-AUTH-003
- `src/lib/schemas/settings.ts` → REQ-SET-001, REQ-SET-002, REQ-SET-003, REQ-SET-004, REQ-SET-005
- `src/lib/schemas/tags.ts` → REQ-SET-002

## Checks that passed (no findings)

- All REQs touched in the diff (REQ-PIPE-008, REQ-MAIL-001/002, REQ-AUTH-001/002/008, REQ-DISC-001/004, REQ-SET-001/007) have at least one test file referencing the REQ ID under `tests/`. No auto-demote.
- No forbidden content introduced in `sdd/*.md` (the only `sdd/` diff besides this run is the new `sdd/.review-decisions.md` file).
- No changelog drift — no pre-existing new entries in `sdd/changes.md`.
- ADR-recorded overrides for mechanism-leakage (REQ-AUTH-002, REQ-AUTH-003, REQ-DISC-001, REQ-DISC-002, REQ-SET-001, REQ-SET-005) and forbidden-content (REQ-MAIL-001) are honored.
- No fake-Deprecated REQs introduced.
- Source-vs-test coverage: every source annotation in the diff has at least one matching test file reference.

## Report summary

- CRITICAL: 0
- HIGH: 1 (REQ-DISC-001 AC 2 spec drift) — auto-fixed
- MEDIUM: 1 (schema REQ annotations) — auto-fixed
- LOW: 0
- Escalated: 0
