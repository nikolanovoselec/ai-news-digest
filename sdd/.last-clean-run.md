# Last `/sdd clean` Run

**Date:** 2026-05-13
**Mode:** unleashed
**Scope:** all (full `sdd/` + `documentation/` corpus) + cycle-4 binding-cap follow-up
**Branch:** develop
**Triggered by:** user (`/sdd clean --all`), continued by `start triage for review needed stuff`

## Pre-run state

Working tree clean. Last committed state was `ee14dfd refactor(spec): split authentication by actor coherence, add rate-limits domain`, which produced a tuned corpus dry-run of 0 HIGH / 2 MEDIUM / 7 LOW.

After the user tightened the discipline framework mid-run (`it cannot happen to qpprove of a 17 AC requirement, or a 10 AC requirement`), the AC-count cap rule was rewritten to be binding: 8-10 ACs single-actor + single-concern flipped from LOW informational to MEDIUM mandatory-split; >10 ACs flipped from MEDIUM escalate to HIGH mandatory-split. The "no axis, defer to owner" JUDGMENT escape hatch was replaced by a deterministic sub-feature split algorithm (lexical clustering of AC first-clause subjects with median fallback) so the cap binds in every case. All four LOW-band length smells from the initial run were re-triaged as mandatory splits and applied in the same session.

## Findings (post-binding-cap follow-up)

| Severity | Count | Auto-fixed | Escalated | Informational |
|---|---|---|---|---|
| CRITICAL | 0 | - | - | - |
| HIGH     | 0 | - | - | - |
| MEDIUM   | 6 | 6 | 0 | - |
| LOW      | 4 | 1 | - | 3 |

### Auto-fixed (SAFE / mechanical splits)

1. **`sdd/generation.md` - REQ-PIPE-001 AC 8 sub-bullet promotion** (MEDIUM, `sub-bullets-banned`). The five `a./b./c./d./e.` sub-bullets describing body-fetch behaviour were promoted to top-level ACs. AC 5 (tautology with Intent) was folded into the Intent paragraph. Final shape: REQ-PIPE-001 retains 7 ACs covering coordinator-level orchestration; the body-fetch sub-feature extracted to REQ-PIPE-010 (6 ACs); candidate filtering rules (published-at parsing, stale-date drop, blocklist) extracted to REQ-PIPE-011 (3 ACs).
2. **`sdd/generation.md` - REQ-PIPE-003 split by sub-feature** (HIGH under new binding cap, 18 ACs → 4 REQs). REQ-PIPE-003 keeps the core matching contract (7 ACs: canonicalisation, same-event collapse, survivor + preserve, single-source baseline, aggregator-wrapper, wire-syndicated tie-break, bidirectional newly-arrived / already-stored). REQ-PIPE-012 carries matching policy variants (3 ACs: distinct methodology, same-publisher stricter threshold, same-news-cycle window). REQ-PIPE-013 carries cross-tick automation + retention coupling (4 ACs). REQ-PIPE-014 carries operator dedup surfaces (4 ACs: full-pool sweep, pair-similarity diagnostic, embedding regen, partial-outage handling).
3. **`sdd/email.md` - REQ-MAIL-001 split by sub-feature** (MEDIUM under new binding cap, 10 ACs → 2 REQs). REQ-MAIL-001 keeps the content contract (7 ACs: subject, body article list, since-midnight tally, send-time pair, manage-notifications link, brand footer, From header). REQ-MAIL-003 carries the send policy (4 ACs: once-per-day gate, configured local time, opt-out, zero-articles silence).
4. **`sdd/reading.md` - REQ-READ-007 split by sub-feature** (MEDIUM under new binding cap, 9 ACs → 2 REQs). REQ-READ-007 keeps the pop / hold / cascade motion sequence (6 ACs). REQ-READ-008 carries scroll, wrap, and fallback behaviour (3 ACs).
5. **`documentation/decisions/README.md` - AD9 Overrides extended** (MEDIUM, `forbidden-content-column-name` re-binding after split). Added `forbidden-content-column-name:REQ-MAIL-003` alongside the existing MAIL-001 override; updated the AC-reference cite from "MAIL-001 AC 9" to "MAIL-003 AC 3" since the `email_enabled` AC moved with the send-policy split.
6. **`documentation/decisions/README.md` - AD9 Context paragraph updated** (no severity, audit trail). Added one sentence recording the 2026-05-13 split rationale and the override extension.

### LOW auto-fix carried over from initial run

1. **`documentation/architecture.md` - file-level `doc-allow-large` marker** (LOW, Pass 2 file-budget). File at 442 lines exceeded the 350-line Pass 2 budget. Added `<!-- doc-allow-large: AD46 architecture-doc diagram-section exemption -->` after the audience line. AD46b is the existing carveout that justifies the size (load-bearing ASCII diagrams require contiguous prose context). The three element-level markers at the diagrams remain.

### Documentation re-anchoring (mechanical)

- `documentation/configuration.md` MAIL-001 anchor updated from `req-mail-001-digest-ready-email` to `req-mail-001-digest-ready-email-content`; cron-schedule entry re-pointed at REQ-MAIL-003.
- `documentation/architecture.md` MAIL-001 anchor updated everywhere; dispatcher-concerned modules (`email-data.ts`, `email-dispatch.ts`, `tz.ts`, `src/worker.ts`) re-pointed at REQ-MAIL-003; renderer-concerned modules (`email-html.ts`, `hashtags.ts`, `json-string-array.ts`) stay on MAIL-001; `email.ts` (renderer + transport) references both.
- `documentation/architecture.md` `src/components/TagStrip.astro` row extended to reference both REQ-READ-007 and REQ-READ-008.
- `documentation/decisions/README.md` AD19 Related-requirements anchor repaired (pre-existing stale `req-read-007-tag-railing-cascade-on-toggle-and-add` corrected to current heading slug) and extended with REQ-READ-008.

### Informational (no auto-fix per rule)

- `sdd/generation.md` - REQ-PIPE-014: 26 lines. Genuine length for an operator-surfaces REQ that lists four distinct admin tooling guarantees; no AC count issue (4 ACs).
- The 4 LOW-band length smells from the initial run (REQ-MAIL-001 10 ACs, REQ-PIPE-001 16 ACs post-promotion, REQ-PIPE-003 17 ACs, REQ-READ-007 8 ACs) are all now resolved by the splits above.

## Test-name re-cites applied in the same session (Cat A from `.review-needed.md` triage)

- `tests/pipeline/force-refresh.test.ts` - REQ-OPS-005 cite added to both `describe` blocks at lines 263 and 349.
- `tests/settings/discovery-status.test.ts` - REQ-DISC-002 added alongside the existing REQ-SET-006 cite at lines 136 and 162.
- `tests/reading/digest-page.test.ts` - REQ-READ-005 AC 3 added alongside the existing REQ-READ-001 cite at line 38.
- `tests/settings/api.test.ts` - REQ-SET-005 AC 2 added alongside the existing REQ-SET-001 cite at line 215.

DES-001 was checked and skipped: no source code references it directly, and the test that would cite it has not been authored yet.

## Rules applied this run

- `spec-discipline.md` - Binding AC-count cap (the new tightened rule), Splitting by sub-feature with lexical clustering + median fallback (the new deterministic algorithm), Sub-bullets in ACs banned, Cross-cutting concerns get their own REQ family.
- `spec-discipline.md` - Rule A (stub-after-extraction), Rule B (Notes two-shape), Rule C (preamble edit-history): zero new findings across the corpus. Seven Notes fields initially added during the splits were dropped after Rule B detected anchor-link form violations (they used same-file `[REQ-PIPE-N](#anchor)` syntax instead of the required `documentation/**` / `sdd/**` paths). The Dependencies field carries the post-split relationships instead.
- `documentation-discipline.md` - Pass 2 file-budget enforcement (file-level `doc-allow-large` marker added per AD46b); Pass 10 stale-link detection drove the doc anchor re-pointing above.
- `spec-discipline.md` - Deprecated-REQ carveout held (no structural findings emitted on Deprecated REQs).
- `~/.claude/rules/spec-discipline.md` itself was updated in this session to make the AC-count cap binding and to add the sub-feature split algorithm; that file is a local user-home artifact and is not committed to the project repo.

## Working tree changes

- `documentation/architecture.md` - file-level `doc-allow-large` marker; MAIL-001 and READ-007 backlink anchors updated; TagStrip row extended with READ-008.
- `documentation/configuration.md` - MAIL-001 backlink anchor updated; cron schedule re-pointed at MAIL-003.
- `documentation/decisions/README.md` - AD9 Overrides + Context extended for MAIL-003; AD19 Related-requirements anchor repaired and extended with READ-008.
- `sdd/generation.md` - REQ-PIPE-001 / -003 splits; REQ-PIPE-010, REQ-PIPE-011, REQ-PIPE-012, REQ-PIPE-013, REQ-PIPE-014 introduced.
- `sdd/email.md` - REQ-MAIL-001 split; REQ-MAIL-003 introduced.
- `sdd/reading.md` - REQ-READ-007 split; REQ-READ-008 introduced.
- `sdd/changes.md` - five new entries under 2026-05-13 recording the splits.
- `sdd/.review-needed.md` - 2026-05-13 follow-up section appended documenting the resolution of all four LOW-band length smells.
- `sdd/.last-clean-run.md` - this file overwritten.
- `tests/pipeline/force-refresh.test.ts`, `tests/settings/discovery-status.test.ts`, `tests/reading/digest-page.test.ts`, `tests/settings/api.test.ts` - test-name re-cites.

## Next steps

- Per-category `[sdd-clean]` commits on `develop`; CI monitor; await user merge to `main`.
- After CI green and merge, the binding-cap cluster is closed.
