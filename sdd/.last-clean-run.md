# spec-reviewer run — 2026-04-27 (refresh-tokens push)

**Mode:** unleashed
**Trigger commit:** 94e862d (`feat(auth): access + refresh tokens with device-bound rotation`)
**Branch:** feat/refresh-tokens (not main/master — unleashed permitted, no `--branch-confirmed` required)
**Round counter:** 0/3 (last 3 commits: `94e862d` (user), `e205788` (user), `2753cf2` (user) — zero agent-authored, well under the 2/3 spiral limit)

## Phase 0 — Triage

- SDD bootstrap detected (`sdd/README.md` present)
- Config: `mode: unleashed`, `enforce_tdd: true` (forced)
- Working tree clean
- User overrides parsed: 3 entries (REQ-DISC-002, REQ-SET-004 deprecated-with-live-code; REQ-MAIL-001 column-name allowance) — none match findings in this run
- Round counter under limit
- Diff classification: **behavioral.** `git diff origin/main...HEAD` covers 64 commits with 6,538 inserts / 1,955 deletes spanning auth (access/refresh-token model), pipeline (idempotent counters, finalize-lock rollback), discovery, settings, observability. spec content already updated in the same push — Phase 1 sync work was done by the user before push.

## Phase 1 — Sync

No spec edit needed. The push is **spec-led** — the user wrote the REQ updates first and then implemented the code:

- **REQ-AUTH-002 rewritten** (single 1-hour JWT → 5-min access + 30-day refresh). Source (`src/middleware/auth.ts`, `src/lib/session-jwt.ts`, `src/lib/refresh-tokens.ts`, `src/pages/api/auth/refresh.ts`, `src/pages/api/auth/logout.ts`, `src/pages/api/auth/[provider]/callback.ts`, `migrations/0009_refresh_tokens.sql`) all annotated.
- **REQ-AUTH-008 added** (refresh-token rotation, device fingerprint, reuse detection). Same source annotations + dedicated test file `tests/auth/refresh-tokens.test.ts` (12 tests across 5 ACs).
- **REQ-AUTH-001 AC 8 + AC 9 added** (three-layer admin gate + `/api/auth/*` rate limiting). Source `src/middleware/admin-auth.ts`, `src/lib/rate-limit.ts` annotated; tests in `tests/middleware/admin-auth.test.ts` and across mutation endpoint tests.
- **REQ-OPS-005 added** (admin force-refresh contract). Source `src/pages/api/admin/force-refresh.ts` annotated; tests in `tests/pipeline/force-refresh.test.ts`.
- **REQ-PIPE-005 / REQ-HIST-001** (7→14 day window) — already sync'd in prior rounds.
- **REQ-PIPE-006 AC 4/6/7, REQ-PIPE-008 AC 9** — already sync'd in prior rounds.

## Phase 2 — Validate

- **Forbidden content (rule 1):** scanned changed REQs (AUTH-001, AUTH-002, AUTH-008, OPS-005). HTTP codes (`429`, `303`, `200`, `401`, `403`), header names (`Cf-Access-Jwt-Assertion`, `Retry-After`, `Accept`), endpoint paths (`/api/admin/*`, `/api/auth/*`), env var (`CF_ACCESS_AUD`), and small JSON shapes (`{ ok: true, scrape_run_id, reused }`) all appear. **Allowed** under existing project convention — same pattern is used in REQ-AUTH-003 AC 2, REQ-GEN-002 AC 2/4, and `forbidden_content_allowlist.http_codes_in_api_reqs: true` in `sdd/config.yml`. Cookie attribute names (`HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`, `__Host-` prefix) ARE the security contract for these REQs, not implementation leakage. No finding.
- **REQ length (rule 2):** REQ-AUTH-001 = 24 lines, REQ-AUTH-002 = 19 lines, REQ-AUTH-008 = 18 lines, REQ-OPS-005 = 20 lines. All under the 25-line warn threshold.
- **Status discipline (rule 3):** all four touched REQs have one-word `Implemented`. No prose Status anywhere in `sdd/`.
- **Fake-Deprecated (rule 4):** no Deprecated REQs in this push; the existing Deprecated REQs in `sdd/generation.md` (REQ-GEN-002/007/008/004) all carry `Replaced By:` or `Removed In:` fields per the override file.
- **Auto-demote (rule 5a):** all four REQs have ≥1 test reference. No demotion.
- **Source-vs-test coverage (rule 5b):**
  - REQ-AUTH-001: source + tests ✓
  - REQ-AUTH-002: source + tests ✓
  - REQ-AUTH-008: source (7 files) + tests (12 tests in `tests/auth/refresh-tokens.test.ts` + cross-references in `middleware.test.ts`) ✓
  - REQ-OPS-005: source + tests ✓
- **Test quality (rule 5c):**
  - REQ-AUTH-008: 5 ACs, 7+ tests citing the REQ. Count gate passes (7 ≥ 5). Pattern scan: no tautologies (`expect(true).toBe(true)`), no `.skip`, no empty bodies. **Sub-case gap:** AC 3 (logout revokes only active row, not all rows for user) has source annotation + inline comment but no dedicated test asserts the multi-device invariant — the count heuristic does not catch this. Logged to `.review-needed.md` for `tdd-guide` follow-up; not auto-fixable by spec-reviewer.
  - REQ-AUTH-001: 9 ACs, ≥9 tests citing the REQ. AC 8 + AC 9 both have direct tests. OK.
  - REQ-AUTH-002: 4 ACs, multiple tests per AC. OK.
  - REQ-OPS-005: 5 ACs, 5+ tests (file header explicitly enumerates AC 1+2/3/4 mappings). OK.
- **Format compliance (rule 6):** all four REQs have ID, Intent, Applies To, AC, Constraints, Priority, Dependencies, Verification, Status. ✓
- **Cross-references (rule 7):** REQ-AUTH-002 → REQ-AUTH-008 ✓; REQ-AUTH-008 → REQ-AUTH-002 ✓; REQ-OPS-005 → REQ-PIPE-001, REQ-AUTH-001 ✓; REQ-AUTH-001 (in observability.md) ✓; REQ-AUTH-007 (referenced in REQ-AUTH-001 AC 7) ✓.
- **Constraints (rule 8):** CON-AUTH-001, CON-SEC-001 both defined in `sdd/constraints.md`. ✓
- **Domain consistency (rule 9):** all 10 domains in `sdd/README.md` have files. ✓
- **No duplicate REQs (rule 10):** REQ-AUTH-008 defined only once. ✓
- **Strikethrough (rule 11):** none in `sdd/`. ✓
- **"Current implementation:" branches (rule 12):** none. ✓
- **Changelog discipline:** entries on 2026-04-27 are dated, ≤2 sentences, user-facing. The four AUTH-related entries (REQ-AUTH-001/002/008, REQ-OPS-005) are correctly classified as new requirements / changed AC, not cleanup — they belong here per the discipline rule.

## Phase 3 — Apply

Unleashed mode JUDGMENT calls: none triggered.

LOW/MEDIUM findings on `sdd/`: none auto-fixable. The one residual MEDIUM (REQ-AUTH-008 AC 3 coverage gap) is logged in `.review-needed.md` because the fix is a test, which is `tdd-guide`'s lane.

`.review-needed.md` cleared the two RESOLVED entries from the prior round (REQ-OPS-005 force-refresh REQ, and the 7→14 day prose drift), replaced with the single new entry above.

## Phase 4 — Changelog

No new changelog entry. The user already wrote a comprehensive, well-formed `## 2026-04-27` block covering REQ-AUTH-001/002/008, REQ-OPS-005, REQ-PIPE-005/006/008, REQ-HIST-001 changes. Each line meets the discipline rule (dated, ≤2 sentences, user-facing, no commit SHA, describes user-visible behaviour change).

## Phase 5 — Report

```
spec-reviewer report — mode: unleashed
  CRITICAL: 0
  HIGH:     0
  MEDIUM:   1 (REQ-AUTH-008 AC 3 — escalated to .review-needed.md)
  LOW:      0
  Auto-fixed: 1 (cleared 2 RESOLVED entries from .review-needed.md, replaced with the new AC 3 entry)
  Escalated to .review-needed.md: 1 new
  Round counter: 0/3
```

## Per-category commit plan

Single commit, single category: `chore(sdd): refresh review-needed.md and audit log for refresh-tokens push`.
