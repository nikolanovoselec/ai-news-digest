# spec-reviewer run — 2026-04-27 (round 2)

**Mode:** unleashed
**Trigger commit:** a0a345c (fix(pipeline): gate addChunkStats on first completion INSERT)
**Branch:** review-2026-04-27/concurrency-correctness (not main/master — unleashed permitted)
**Round counter:** 1/3 (most recent commit is non-agent; counter reset)

## Phase 0 — Triage

- SDD bootstrap detected (`sdd/README.md` present)
- Config: mode=unleashed, enforce_tdd=true (forced)
- Working tree clean
- Round counter under limit
- Diff classification: behavioral — `processOneChunk` reordered so the chunk-completion INSERT runs before `addChunkStats`, and the additive UPDATE is gated by `completionResult.meta.changes === 1`. Companion test pins the contract (two `processOneChunk` calls for the same `(run_id, chunk_index)` pair issue exactly one stats UPDATE).

## Phase 1 — Sync

**Added REQ-PIPE-006 AC 7** (scrape_runs aggregation — Implemented):

> The per-tick token, cost, articles-ingested, and articles-deduplicated counters advance exactly once per chunk regardless of how many times the queue redelivers that chunk's message. A redelivered chunk that has already been recorded as completed for the run leaves these counters at their existing values, so the stats widget and history page never show inflated tokens, cost, or article counts attributable to retry traffic rather than real LLM work.

Rationale: REQ-PIPE-006 AC 6 already pinned finish-time idempotency under redelivery; the symmetric guarantee for the per-chunk counters was missing from the spec but was just shipped as code in `a0a345c`. This brings the spec back in line with the observable contract. Domain placement: REQ-PIPE-006 owns the stats counters, so AC belongs there (not REQ-PIPE-002, which owns the LLM I/O contract).

## Phase 2 — Validate

- Forbidden content: clean. AC names "tokens", "cost", "articles-ingested", "articles-deduplicated" — these are the user-facing counters surfaced on the stats widget and history page, not implementation column names. No file paths, function names, or SQL.
- REQ length: REQ-PIPE-006 grew from 22 to 24 lines — OK (under 25 threshold).
- Status discipline: `Implemented` — one word — OK.
- Auto-demote (5a): REQ-PIPE-006 has multiple test references (`scrape-run.test.ts`, `scrape-status.test.ts`); no demotion.
- Source-vs-test (5b): REQ-PIPE-006 source annotated in `src/lib/scrape-run.ts`, `src/lib/ulid.ts`, `src/pages/api/scrape-status.ts`. Tests reference it. The new behavior is functionally pinned by a test in `chunk-consumer.test.ts` whose title currently says REQ-PIPE-002 — see Phase 3 escalation.
- Test quality (5c): REQ-PIPE-006 has 7 ACs and well over 7 test functions referencing it. Within budget. No tautology patterns or skipped tests detected on this REQ.
- Format compliance: all required fields present.
- Cross-references resolve (CON-DATA-001, REQ-PIPE-001).
- No duplicate REQs.

## Phase 3 — Apply

- Edit applied on current branch (`review-2026-04-27/concurrency-correctness`).
- Single commit per spec-discipline grouping (one category: REQ-PIPE-006 AC addition).
- 1 MEDIUM finding escalated to `sdd/.review-needed.md`: the new test's title says `REQ-PIPE-002` but the contract it pins now lives in REQ-PIPE-006 AC 7. Renaming the test would touch test code (out of lane), so this is recorded for user judgment.

## Phase 4 — Changelog

Added entry under `## 2026-04-27`:

> REQ-PIPE-006 AC 7 added: the per-tick token, cost, articles-ingested, and articles-deduplicated counters now advance exactly once per chunk regardless of how many times the queue redelivers that chunk's message, so a flaky tick can no longer inflate the stats widget or history page with retry traffic that was never real LLM work.

## Phase 5 — Report

| Severity | Count | Notes |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 1 | Test title cross-REQ mismatch — escalated |
| LOW | 0 | — |
| Auto-fixed | 1 | REQ-PIPE-006 AC 7 added |
| Escalated | 1 | Test rename judgment for the user |

No changes to `documentation/` or source code (out of lane). Doc-updater runs next and may want to extend `documentation/architecture.md`'s coverage of the chunk-consumer idempotency contract.
